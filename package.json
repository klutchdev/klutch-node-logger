{
  "name": "klutch-node-logger",
  "version": "0.0.3",
  "description": "Simple logging for Node CLI apps",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "index.js",
    "index.d.ts"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run clean && npm run build && rollup -c",
    "clean": "rm -rf dist && rm -rf index.js && rm -rf index.d.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kyle \"klutch\" Leary",
  "license": "ISC",
  "dependencies": {
    "chalk": "^4.1.0",
    "klutch-node-logger": "^0.0.3",
    "log-symbols": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^16.4.6",
    "rollup": "^2.55.0",
    "rollup-plugin-dts": "3.0.2",
    "typescript": "^4.3.5"
  }
}
